query GetCourseWithLessons($id: String!) {
  course(id: $id) {
    id
    title
    slug
    description
    imageUrl
    userId  # 🆕 AJOUTER CE CHAMP!
    chapters {
      id
      title
      position
      lessons {
        id
        title
        order
        duration
        videoUrl
        externalVideoUrl
        content
        description
        completed
      }
    }
  }
}
