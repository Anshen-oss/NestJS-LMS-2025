mutation CreateLessonAttachment($input: CreateLessonAttachmentInput!) {
 createLessonAttachment(input: $input) {
      id
    lessonId
    fileName
    fileUrl
    fileSize
    fileType
    createdAt
 }
}
